Instructions to Recreate the Battery Notificator Program:  1. Download and install Python version 3.7 or higher from python.org. 2. Install the necessary packages, including psutil, py-notifier, and win10toast. 3. In your command line or terminal, navigate to the directory containing the script (battery.py). 4. Run the script using Python:    `ash     python battery.py    ` 5. The script will check the current battery percentage of the device.  6. If the battery level is at or below 30% and the device is not plugged in, it will trigger a desktop notification containing the current battery percentage and a message indicating the battery is low.